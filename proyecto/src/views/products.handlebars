<div class="navbar-container">
  <h2 class="title">Productos </h2>
  <div class="navbar">
    {{#if adminUser}}
      <a href="/"> Home Page</a>
      <a href="/sessions/logout">Logout</a>
    {{else}}
      <a href="/"> Home Page</a>
      <a href="/cart/{{user.cart}}">Cart</a>
      <a href="/sessions/logout">Logout</a>
    {{/if}}
  </div>
</div>

<hr />
<br />

<p
  id="welcome-message"
  data-userid="{{user._id}}"
  data-cart="{{user.cart}}"
>Bienvenido,
  {{user.role}}:
  {{user.first_name}}
  {{user.last_name}}</p>

<br />
<div id="product-list" class="list-container">
  {{#each products}}
    <ul id="{{_id}}" class="products">
      <p class="title-product">{{title}}</p>
      <li class="img-product">{{thumbnail}}</li>
      <li id="price" data-price="{{price}}">${{price}}</li>
      <li><strong>Descripción:</strong> {{description}}</li>
      <li><strong>Categoría:</strong> {{category}}</li>
      <button
        type="button"
        class="btn-add-cart"
        data-owner="{{owner}}"
        data-id="{{_id}}"
      >🛒</button>
    </ul>

  {{/each}}
</div>

<script src="/js/addToCart.js"></script>