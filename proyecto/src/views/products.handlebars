<div class="navbar-container">
  <h2 class="title">Productos </h2>
  <div class="navbar">
    <a href="/cart/carts/6607ae06feb283d3a93e4118">Cart</a>
    <a href="/"> Home Page</a>
  </div>
</div>

<hr />

<div id="product-list" class="list-container">
  {{#each products}}
    <ul id="{{_id}}" class="products">
      <p class="title-product">{{title}}</p>
      <li class="img-product">{{thumbnail}}</li>
      <li>${{price}}</li>
      <li><strong>Descripción:</strong> {{description}}</li>
      <li><strong>Categoría:</strong> {{category}}</li>
      <button type="button" class="btn-add-cart" data-id="{{_id}}">🛒</button>
    </ul>

  {{/each}}
</div>

<script src="/js/addToCart.js"></script>